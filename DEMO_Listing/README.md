# OMNE Demo Listing Deploy

This is the trimmed deploy project for the standalone demo listing.

- `/` redirects to `/listing-demo/`
- `/listing-demo/` serves the brokerage listing replica
- `/portfolio` serves the OMNE Mount used inside the listing iframe

Deploy this folder to Vercel/GitHub, not the larger working project.
